# Extract poses 
cd openpose_keras &&
python3 demo_image_extract_pose.py --input-folder=

# Train the spatial temporal network
python3 spatial_temporal_nn.py --epochs= --initial-epochs= --num-workers=

# Train the motion temporal network
python3 motion_temporal_nn.py --epochs= --initial-epochs= --num-workers=
